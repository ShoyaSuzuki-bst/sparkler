class Topic {
  String id = '';
  String title = '';
  String content = '';
  DateTime createdAt = DateTime(0);

  Topic(
    String id,
    String title,
    String content,
    DateTime createdAt,
  );
}